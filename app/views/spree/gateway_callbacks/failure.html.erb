<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @heading %></title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      line-height: 1.5;
      color: #333;
      background-color: #f5f5f5;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 40px auto;
      padding: 32px;
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 2px 12px rgba(0,0,0,0.07);
      text-align: center;
    }
    h1 {
      color: <%= @color %>;
      margin-top: 0;
    }
    .message {
      margin: 18px 0 0 0;
      font-size: 1.2em;
    }
    .details {
      margin: 24px auto 0 auto;
      font-size: 1em;
      text-align: left;
      max-width: 400px;
    }
    .details table {
      width: 100%;
      border-collapse: collapse;
    }
    .details td {
      padding: 8px 12px;
      border-bottom: 1px solid #eee;
    }
    .details tr:last-child td {
      border-bottom: none;
    }
    .details tr td:first-child {
      font-weight: bold;
      white-space: nowrap;
      width: 120px;
    }
    .actions {
      margin-top: 32px;
      display: flex;
      gap: 16px;
      justify-content: center;
    }
    .btn {
      display: inline-block;
      padding: 12px 28px;
      border-radius: 6px;
      text-decoration: none;
      font-weight: bold;
      font-size: 1em;
      border: none;
      cursor: pointer;
      text-align: center;
    }
    .btn-primary {
      background: #1976d2;
      color: #fff;
    }
    .btn-secondary {
      background: <%= @color %>;
      color: #fff;
    }
    .btn:hover {
      opacity: 0.9;
    }
    
    /* Error X Animation */
    .error-animation {
      width: 80px;
      height: 80px;
      margin: 0 auto 24px;
      position: relative;
    }
    
    .error-icon {
      width: 80px;
      height: 80px;
      position: relative;
      border-radius: 50%;
      box-sizing: content-box;
      border: 4px solid #d32f2f;
      animation: error-icon-anim 0.75s ease-in-out;
    }
    
    .error-line {
      position: absolute;
      height: 5px;
      width: 50px;
      background-color: #d32f2f;
      display: block;
      top: 37px;
      left: 15px;
      border-radius: 2px;
    }
    
    .error-line--left {
      transform: rotate(45deg);
      animation: error-line-left 0.75s ease-in-out;
    }
    
    .error-line--right {
      transform: rotate(-45deg);
      animation: error-line-right 0.75s ease-in-out;
    }
    
    @keyframes error-icon-anim {
      0% {
        transform: scale(0.3);
        opacity: 0;
      }
      50% {
        transform: scale(1.1);
      }
      75% {
        transform: scale(0.9);
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }
    
    @keyframes error-line-left {
      0% {
        transform: rotate(0deg) scale(0.1);
        opacity: 0;
      }
      50% {
        transform: rotate(0deg) scale(0.1);
        opacity: 0;
      }
      75% {
        transform: rotate(45deg) scale(1.1);
      }
      100% {
        transform: rotate(45deg) scale(1);
        opacity: 1;
      }
    }
    
    @keyframes error-line-right {
      0% {
        transform: rotate(0deg) scale(0.1);
        opacity: 0;
      }
      50% {
        transform: rotate(0deg) scale(0.1);
        opacity: 0;
      }
      75% {
        transform: rotate(-45deg) scale(1.1);
      }
      100% {
        transform: rotate(-45deg) scale(1);
        opacity: 1;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="error-animation">
      <div class="error-icon">
        <span class="error-line error-line--left"></span>
        <span class="error-line error-line--right"></span>
      </div>
    </div>
    <h1><%= @heading %></h1>
    <p class="message"><%= @message %></p>
    <div class="details">
      <%= raw @details %>
    </div>
    <div class="actions">
      <a href="<%= @payment_path %>" class="btn btn-primary">Retry Payment</a>
      <a href="<%= @root_path %>" class="btn btn-secondary">Return to Store</a>
    </div>
  </div>
</body>
</html>
